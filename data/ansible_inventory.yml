all:
  children:
    network:
      children:
        routers:
          hosts:
            demo-router1:
              ansible_host: 192.168.1.1
              ansible_network_os: ios
              ansible_password: admin123
              ansible_user: admin
        switches:
          hosts:
            demo-switch1:
              ansible_host: 192.168.1.2
              ansible_network_os: ios
              ansible_password: admin123
              ansible_user: admin
